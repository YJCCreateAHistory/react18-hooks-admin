import React from "react";
import { RoutesAssembly } from "../type";
import { LayoutIndex } from "../../layout";
export const authRoutesAssembly: Array<RoutesAssembly> = [
  {
    element:<LayoutIndex></LayoutIndex>,
    meta:{
      title:"组件"
    },
    children:[
      {

      }
    ]
  }
]
